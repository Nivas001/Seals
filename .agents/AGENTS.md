# Workspace Rules

> [!IMPORTANT]
> **Always Commit and Push:** At the end of every task or set of changes in this workspace, you MUST automatically run `git add .`, `git commit -m "..."`, and `git push` to GitHub without waiting for the user to explicitly ask you to do so.
